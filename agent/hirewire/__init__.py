"""HireWire agent — multi-tenant SignalWire AI virtual-employee backend."""

# TODO(phase-2): once `agent/` is an installable package, source __version__
# from importlib.metadata.version("hirewire-agent") so this string and the
# version in pyproject.toml cannot drift.
__version__ = "0.1.0"
