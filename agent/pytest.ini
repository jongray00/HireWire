# cov-fail-under is 0 during scaffolding; raise to 80 once Phase 1 + Phase 2 are landed
[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --strict-markers --tb=short --cov=lib --cov-report=term-missing --cov-fail-under=0
markers =
    integration: marks integration tests
